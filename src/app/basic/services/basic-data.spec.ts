import { BasicDataService } from './basic-data.service';
let service: BasicDataService;
describe('Basic data service', () => {
    beforeEach(() => {
        // service = new BasicDataService();
    })
})